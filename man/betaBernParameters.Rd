% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/betaBernParameters.R
\name{betaBernParameters}
\alias{betaBernParameters}
\title{Estimate Beta-Bernoulli Parameters from LLM Output}
\usage{
betaBernParameters(llmobject, method = "mle", force_mom = FALSE)
}
\arguments{
\item{llmobject}{An object of class \code{"elicitEdgeProb"} or \code{"elicitEdgeProbLite"},
as returned by the functions \code{"elicitEdgeProb"} or \code{"elicitEdgeProbLite"}.}

\item{method}{Estimation method. One of \code{"mle"} (maximum likelihood) or \code{"mom"} (method of moments).
Default is \code{"mle"}.}

\item{force_mom}{Logical. If \code{TRUE}, forces method of moments estimation even if \code{"mle"} is requested.
Default is \code{FALSE}.}
}
\value{
A list containing the estimated \code{alpha} and \code{beta} parameters of the Beta-Bernoulli distribution.
}
\description{
This function estimates the parameters of a Beta-Bernoulli distribution from the edge
inclusion probabilities elicited using the functions \code{"elicitEdgeProb"} or \code{"elicitEdgeProbLite"}.
These parameters help in describing the prior probability for the network density.
The elicited parameters can be used for specifying the shape parameters
of the Beta-Bernoulli structure prior in the package \link[easybgm:easybgm]{easybgm}.
}
\details{
The function extracts the number of included edges (\code{"I"}) for each permutation or repetition
from the LLM output and fits a Beta-Bernoulli distribution to these counts. It estimates the
shape parameters \code{alpha} and \code{beta} based on the specified method. The available estimation methods are:
\itemize{
\item \code{"mle"}: Maximum likelihood estimation.
\item \code{"mom"}: Method of moments estimation.
A warning is issued if fewer than 5 permutations are detected, as parameter estimation
may be unreliable in such cases.
}
}
\examples{
\dontrun{
llm_out <- llmPriorElicitSimple(
  context = "Exploring cognitive symptoms and mood in depression",
  variable_list = c("Concentration", "Sadness", "Sleep"),
  n_rep = 3
)
beta_params <- betaBernParameters(llm_out)
print(beta_params)
}

}
\seealso{
\link[easybgm:easybgm]{easybgm}
}
